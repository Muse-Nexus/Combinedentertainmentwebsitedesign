# Raining Entertainment — client closeout receipt

Date: 2026-08-17 (Pacific/Honolulu)
Implementation commit: `91ffa71` on `codex/raining-client-audio-closeout`
Production deployment: `https://combinedentertainmentwebsitedesign-1mkazv26x.vercel.app` → aliased to **https://www.rainingentertainment.com** (existing Vercel project `combinedentertainmentwebsitedesign`, team `mark-matthews-projects`)

## Client requests → implementation

| # | Request (source) | Implementation | Source image |
|---|---|---|---|
| 1 | Replace blurry image in `Screenshot …6.45.34 AM.png` with `7E67D76B…jpeg` (Brenton) | The blurry image was the live Airtable "Game Shows & Casino" moment (`/media/casino-gameshow/casino-group-photo.jpg`, 300×216). Airtable Moments record `recqIJ19OXW2GrJ3H`: Image URL → `/media/casino-gameshow/brenton-craps-gameshow-fanatics-portrait.webp`, Account/Post URL → `@gameshowfanatics`, new alt text. Checked-in fallback updated to match: `shared/site-content.ts` (game-show entry), seed `scripts/setup-airtable.mjs`, `docs/airtable-templates/moments.csv` | `email-assets/7E67D76B-F53A-405D-A3AC-2732761D7C1E_1_105_c.jpeg` (804×978, bit-identical to client email original in `client-assets/email-selected/brenton-july-photos/01-…`), converted full-frame to webp |
| 2 | Same page: replace Jolie image in `…6.48.26 AM.png` with the approved shaka-in-balloons photo | Live Airtable "Cirque" moment `recLEvaPBS7jfR66T`: Image URL → `/media/about/jolie-shaka-balloons-portrait.webp` + new alt. Fallback in `shared/site-content.ts` (cirque-jolie entry), seed + CSV updated. No AI generation — a 4:5 crop of the client-approved photo | Repo original `public/media/client-selected/about/jolie-shaka-balloon-crown.webp` (from client email `client-assets/email-selected/about-jolie/03-AE2B4FF0…`); new 579×724 portrait crop keeps face + shaka hand fully in frame |
| 3 | `…6.50.52 AM.png` kid photos don't read as Corporate — remove/replace | Both identified and replaced in `src/app/pages/Corporate.tsx` gallery: `500194188…n.jpg` → `client-selected/casino/casino-dealer-team-selfie.webp` ("Casino NITE Dealer Team"), `500304533…n.jpg` → `client-selected/casino/casino-full-venue-setup-email.webp` ("Casino Floor Setup"). Both replacements are adult/venue imagery and were simultaneously **removed from the Casino page gallery**, so no photo appears on two pages | Existing approved client-selected casino assets (visually verified adults-only) |
| 4 | Casino Craps card (`…6.54.39`) should use the Brenton craps image (`…6.54.25`) | `src/app/pages/Casino.tsx` Craps card → `/media/casino-gameshow/brenton-craps-gameshow-fanatics-wide.webp` + descriptive alt. Client's explicit double-placement request satisfies the no-duplicate rule via **two distinct derivatives**: portrait (804×978) on the homepage moment, landscape 4:3 crop (804×603, matching the client's own screenshot framing) on Casino | Same client email original as #1; the repo derivative `client-selected/casino/roulette-dealer-portrait.webp` (same scene, misnamed) was removed from the Casino gallery to avoid duplication |
| 5 | Mulligan's claim (`…6.58.04`) → "South Maui's longest-running magic show", never island-wide | `src/app/pages/Magic.tsx`: paragraph → "…It's South Maui's longest-running magic show. Confirm current dates with the venue."; card → "South Maui's Longest-Running"; stat tile → "Magic Show in South Maui". `src/app/pages/About.tsx` → "South Maui's longest-running live magic show". Build guard added in `scripts/verify-site-content.mjs` (island-wide claim regexes now fail the build; verified they don't false-positive on the new copy) | — |
| 6 | Schedule (`…7.02.27`) must not promise every Thursday; Brenton away Aug 27–Sep 17 | Live Airtable Public Events `recqwnT0wvQpCK4XC` Recurrence Label → **"Normally every Thursday"** (production API verified). Fallback `shared/site-content.ts`, seed, `events.csv` match. `Magic.tsx` "Catch it next Thursday!" → "Normally every Thursday — confirm with the venue." `src/app/pages/MulligansMagicShow.tsx` gained a dated schedule note: hiatus Aug. 27 – Sep. 17, 2026 + confirm-with-venue (single retire-after-Sep-17 string; the buyout is intentionally not marketed as a public show) | — |
| 7 | Replace photo in `…7.23.09` with `7DC14798…jpeg` | Homepage Casino NITE service card `src/app/components/HomeContent.tsx` → `/media/casino-nite/red-lantern-casino-room-maui.webp` + new alt. The red-carpet image it replaced remains only on Corporate (its single remaining use — not in the client note, left per minimal-change) | `email-assets/7DC14798-199F-4137-839F-43ABC19FEE01_1_105_c.jpeg` (900×872), full-frame webp |
| 8 | `@gameshowfanatics` linkage wherever Game Show Fanatics appears; never `@magicbrent` in those contexts; don't fake a live feed | Fixed rule violations: game-show moment (Airtable + fallback) now `@gameshowfanatics`; `Footer.tsx` `@magicbrent` card no longer claims "Game Show NITE" and `/game-show` no longer highlights it. Added: third Footer profile card `@gameshowfanatics` ("Three profiles. One entertainment team.", highlights on `/game-show`), third chip in `LatestMoments.tsx`, Contact page social entry. GameShow page already linked correctly (2 links, kept). `@magicbrent` retained for Magic/Casino/Brenton contexts. **The signed SocialFanout pipeline was not touched** — no fake feed; `/api/instagram` still truthfully 503 `not_configured` |
| 9 | Jolie 8/14: homepage photo approved, "Except Brenton looks a little crazy" | **Open — needs a client asset** (see Remaining). Hero left exactly as Jolie approved; no change shipped |

## Verification receipts

- `npm test`: 7/7 Instagram feed tests pass (privacy/HMAC/allowlist behavior pinned and unchanged).
- `npm run build`: clean — 15 canonical HTML routes, valid discovery files, 26 routable paths, 144 referenced media files all resolve, 94 client-selected derivatives (count unchanged), new forbidden-claim guards active. `git diff --check` clean.
- Browser (headless Chrome, vite preview of the production build): desktop 1440px and mobile 390px full-page captures of `/`, `/casino`, `/corporate`, `/magic`, `/shows/mulligans-magic-show`, `/game-show`. Verified visually: new moment cards (shaka portrait crop keeps face + hand; craps portrait sharp), Casino Craps card framing, Corporate gallery all-adult with all 12 images loading (eager-load re-shoot proved the two "empty" cells were a lazy-load capture artifact), Magic residency copy, hiatus note styling, footer three-profile panel, mobile hero untouched.
- Production after deploy: canonical + all 4 new media URLs + `/magic` + `/shows/mulligans-magic-show` + robots + sitemap → 200; `/api/events` → "Normally every Thursday"; `/api/moments` (plain and cache-busted) → new images/handles; `/api/instagram` → 503 `not_configured` (correct); `/api/inquiries` GET → 405 (write protection intact, no test lead created); deployed bundle contains GA4 `G-J3L84JWV88`, consent gating, and 7 `gameshowfanatics` references. Live homepage screenshot confirms the Airtable-rendered Game Shows & Casino card with `@gameshowfanatics`.
- Legacy domains: `magicbrent.com` / `cirquejolie.com` untouched — no DNS, registrar, alias, or redirect changes. No client emails sent. No credentials exposed or rotated.

## Model / subagent contributions

- **Fable 5 (foreman)**: recon and note→component mapping, all reconciliation decisions, independent md5/dead-code verification of Opus claims, hero decision, browser verification, commit, deploy, live Airtable updates, this receipt.
- **Opus 5 (opus-reviewer)**: opened all 10 email assets + 32 repo images; corrected the blurry-card source to `casino-group-photo.jpg` (300×216) and found the in-repo craps source; located the shaka photo; identified both kid photos; found three md5-identical duplicate pairs (all foreman-verified); hero analysis (no better approved frame exists; CSS repositioning mathematically insufficient).
- **Sonnet 5 (sonnet-implementer)**: produced the 4 webp derivatives and all 14 file edits exactly to the reconciled plan (its session was cut off by a process timeout; the foreman recovered and verified the completed work from the working tree).
- **Haiku (auditor)**: 7-check mechanical audit — links, retired copy, per-file media reference counts, dist output, alt text, untracked files, forbidden-surface diff — all pass.

## Genuinely remaining

1. **Homepage hero Brenton fix (Jolie's caveat) — needs client assets.** The repo has no alternate frame of the patriotic shoot (Opus swept every candidate; the contact sheet has zero patriotic frames), and the hero's ~88px of horizontal overflow makes a CSS focal fix meaningless. An edge-crop that slices Brenton would look like an error and alter the composition Jolie approved. **Recommended ask (via Mark — do not email clients directly): request the original full-resolution patriotic shoot files, or their preferred frame; then swap desktop only, leaving the approved mobile frame untouched.** Generative face editing is off the table per the handoff.
2. **Instagram live-feed activation for `@gameshowfanatics` (and the other accounts).** `/api/instagram` correctly returns 503 `not_configured`. Activation needs Meta/SocialFanout authorization, then: add a `gameshowfanatics` entry to `ACCOUNT_DEFINITIONS` in `server/instagram.ts`, set `SOCIALFANOUT_GAMESHOWFANATICS_CONNECTION_ID` (plus the existing two connection IDs) in Vercel, and update `tests/instagram-feed.test.ts` (it intentionally pins the two-account list today). Until then the curated cards + profile links above are the honest treatment. Also pending authorization: the feed-internal `serviceLabel` for `@magicbrent` still reads "Magic · Game Shows · Casino".
3. **Adjacent risk Brenton did not flag:** `Corporate.tsx` still shows `client-selected/corporate/casino-overhead-busy-room.webp` ("Packed Casino NITE Crowd") — also a youth-group event (dark "Alumni" shirts). Left in place per minimal-change, but he may raise it next round.
4. **Machine-readable schedule:** the Mulligan's `EventSeries` JSON-LD (`routeMetadata.js`) still asserts weekly Thursdays with no hiatus exception. Visible copy carries the caveat everywhere; left as-is to preserve the rich result. Flag for a call.
5. **Sep 18, 2026 cleanup:** retire the one hiatus string in `MulligansMagicShow.tsx`.
6. Pre-existing, out of scope, noted only: `casino-craps-table.jpg` ≡ `Casino-Night-32-1030x579.jpg` (the latter still used on Corporate); `UpcomingShowBanner.tsx` is dead code with a fictitious Seattle show (unimported, not shipped — delete at leisure).
